#!/usr/bin/python3
def update_dictionary(a_dictionary, key, value):
    # This syntax updates the value if key exists, or creates it if it doesn't
    a_dictionary[key] = value
    return a_dictionary
